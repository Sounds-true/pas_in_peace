# Анализ архитектуры PAS Bot

## Текущая архитектура (как работает сейчас)

### 1. Поток обработки сообщений

```
Telegram Message
    ↓
bot.py (handle_message)
    ↓
PII Detection (опционально)
    ↓
Crisis Detection
    ↓
StateManager.process_message()
    ↓
Intent Classification (отключен)
    ↓
Entity Extraction (отключен)
    ↓
State Graph (LangGraph)
    ↓
    ├─→ emotion_check (keyword-based)
    ├─→ route по уровню дистресса:
    │   ├─→ high → crisis_support
    │   ├─→ moderate → moderate_support
    │   └─→ low → casual_chat
    ↓
Technique Selection (TechniqueOrchestrator)
    ↓
Active Listening (с OpenAI GPT-4)
    ↓
Supervisor Agent (проверка качества)
    ↓
Response → Telegram
```

### 2. Модули и компоненты

#### **src/core/bot.py**
- **Роль**: Обработчик Telegram сообщений
- **Функции**:
  - `handle_start()` - приветствие
  - `handle_message()` - основная обработка
  - `_send_crisis_response()` - кризисные ситуации
- **Проблема**: НЕ сохраняет историю диалога, использует только StateManager

#### **src/orchestration/state_manager.py**
- **Роль**: Центральный менеджер состояния и диалога
- **Граф состояний** (LangGraph):
  ```
  START → emotion_check → [crisis_support | moderate_support | casual_chat]
         ↓
  technique_selection → technique_execution
         ↓
  [letter_writing | goal_tracking | continue]
         ↓
  END
  ```
- **Проблема**:
  - Keyword-based emotion detection (примитивный)
  - Не использует полную историю диалога
  - Граф слишком простой, нет персонализации

#### **src/techniques/orchestrator.py**
- **Роль**: Выбор терапевтической техники
- **Логика**:
  ```python
  EmotionalState → Priority Techniques
  - RAGE → IFS Parts Work, Grounding
  - DESPAIR → Active Listening, CBT
  - NEUTRAL → Active Listening
  ```
- **Проблема**: Всегда выбирает Active Listening для низкого дистресса

#### **src/techniques/active_listening.py** (ОБНОВЛЁН)
- **Роль**: Генерация эмпатичных ответов
- **Текущая реализация**:
  ```python
  async def _generate_llm_response():
      client = AsyncOpenAI()
      response = await client.chat.completions.create(
          model="gpt-4-turbo-preview",
          messages=[
              {"role": "system", "content": system_prompt},
              {"role": "user", "content": user_message}
          ],
          max_tokens=300,
          temperature=0.7
      )
  ```
- **System Prompt**:
  ```
  Вы — эмпатичный терапевт для родителей с отчуждением.
  Техника: Active Listening
  - Отразите чувства
  - Задайте уточняющий вопрос
  - НЕ давайте советов
  - 3-5 предложений
  ```
- **Проблема**:
  - НЕ передаётся история диалога (только текущее сообщение)
  - НЕ учитывается контекст предыдущих сообщений
  - Всегда задаёт вопросы, не ведёт к действиям

#### **src/techniques/supervisor_agent.py** (ОБНОВЛЁН)
- **Роль**: Проверка качества ответов
- **Метрики**:
  - Empathy (эмпатия) - min 0.3
  - Safety (безопасность) - min 0.8
  - Accuracy (точность)
  - Therapeutic Value (терапевтическая ценность)
  - Respect Autonomy (уважение автономии)
  - Appropriate Boundaries (границы)
- **Изменено**: INSUFFICIENT EMPATHY теперь warning, не critical

### 3. База данных

#### **Структура таблицы `users`**
```sql
- id: INTEGER PRIMARY KEY
- telegram_id: VARCHAR (уникальный)
- current_state: ConversationState ENUM
- therapy_phase: TherapyPhase ENUM
- emotional_score: FLOAT (0-1, где 1 = хорошо)
- crisis_level: FLOAT (0-1, где 1 = кризис)
- total_messages: INTEGER
- total_sessions: INTEGER
- crisis_incidents: INTEGER
- created_at: TIMESTAMP
- last_activity: TIMESTAMP
- context: JSON (словарь с доп. данными)
- consent_given: BOOLEAN
- data_retention_days: INTEGER
```

#### **Что сохраняется**:
1. ✅ **Метаданные пользователя** (ID, timestamps)
2. ✅ **Эмоциональное состояние** (emotional_score, crisis_level)
3. ✅ **Статистика** (total_messages, total_sessions)
4. ❌ **История сообщений НЕ сохраняется в БД**
5. ❌ **Контекст диалога НЕ персистентен**

#### **В памяти (StateManager.user_states)**:
```python
class UserState:
    user_id: str
    message_history: List[Message]  # LangChain messages
    current_state: ConversationState
    completed_techniques: List[str]
    goals: List[Goal]
    # ... но НЕ сохраняется между перезапусками!
```

### 4. Промпты

#### **Active Listening System Prompt**
```python
f"""Вы — эмпатичный терапевт, специализирующийся на поддержке родителей,
столкнувшихся с отчуждением от детей.

Ваша задача: применить технику активного слушания (Active Listening) —
отразить чувства пользователя, показать что вы их слышите и понимаете,
задать уточняющий вопрос.

Контекст:
- Эмоция: {emotion}
- Уровень дистресса: {distress_level}
- Темы: {', '.join(themes)}

Принципы активного слушания:
1. Отразите чувства и переживания пользователя
2. Покажите эмпатию и понимание
3. Задайте мягкий, уточняющий вопрос
4. НЕ давайте советов на этом этапе
5. Создайте безопасное пространство для выражения чувств

Формат ответа:
- 2-3 предложения отражения чувств
- 1 уточняющий вопрос
- Короткое подтверждение поддержки

Будьте кратки (3-5 предложений), тёплыми и поддерживающими."""
```

**Проблема**: Промпт НЕ получает историю диалога!

---

## Проблемы текущей реализации

### 1. **Отсутствие контекста диалога**
- OpenAI получает ТОЛЬКО текущее сообщение
- История диалога НЕ передаётся в LLM
- Бот не помнит, что говорил ранее

### 2. **Стиль диалога слишком однообразный**
- Всегда задаёт вопросы
- Не переходит к действиям (письма, упражнения)
- Зацикливается на "active listening"

### 3. **Нет прогрессии диалога**
- State graph не учитывает количество сообщений
- Нет перехода от слушания → к действиям
- Нет целей диалога

### 4. **История не сохраняется**
- При перезапуске бота вся история теряется
- Невозможна долгосрочная терапия

---

## Рекомендации по улучшению

### 1. **ПЕРЕДАЧА ИСТОРИИ ДИАЛОГА В LLM** ⭐ КРИТИЧНО

**Текущий код** (active_listening.py:150-157):
```python
messages=[
    {"role": "system", "content": system_prompt},
    {"role": "user", "content": user_message}  # ❌ Только текущее сообщение
]
```

**ИЗМЕНИТЬ НА**:
```python
# Получить историю из context
message_history = context.get("message_history", [])

# Построить messages с историей
messages = [{"role": "system", "content": system_prompt}]

# Добавить последние 5-10 сообщений для контекста
for msg in message_history[-10:]:  # Последние 10 сообщений
    if isinstance(msg, HumanMessage):
        messages.append({"role": "user", "content": msg.content})
    elif isinstance(msg, AIMessage):
        messages.append({"role": "assistant", "content": msg.content})

# Текущее сообщение
messages.append({"role": "user", "content": user_message})

response = await client.chat.completions.create(
    model=settings.openai_model,
    messages=messages,  # ✅ Полная история
    max_tokens=500,
    temperature=0.7
)
```

### 2. **УЛУЧШЕНИЕ SYSTEM PROMPT**

**Добавить**:
- Этапы диалога (понимание → анализ → действия)
- Цель диалога (привести к написанию письма)
- Избегать однообразных вопросов

**Новый промпт**:
```python
system_prompt = f"""Вы — эмпатичный терапевт для родителей с родительским отчуждением.

КОНТЕКСТ ДИАЛОГА:
- Сообщений в диалоге: {context.get('message_count', 0)}
- Эмоция: {emotion}
- Уровень дистресса: {distress_level}
- Темы: {', '.join(themes)}

ЭТАПЫ ДИАЛОГА:
1. Первые 2-3 сообщения: Активное слушание, валидация чувств
2. Следующие 3-5 сообщений: Понимание ситуации, сбор деталей
3. После понимания: Предложить написать письмо ребёнку или упражнения

СТИЛЬ ОТВЕТА:
- Если это первые сообщения: отразите чувства, задайте уточняющий вопрос
- Если уже есть понимание ситуации: суммируйте, предложите следующий шаг
- Избегайте повторения одних и тех же фраз
- Варьируйте структуру ответов

ВАЖНО:
- НЕ давайте юридических советов
- НЕ осуждайте другого родителя
- Поддерживайте, но постепенно ведите к действиям (письма, упражнения)

Будьте тёплыми, но конкретными. 3-5 предложений."""
```

### 3. **ДОБАВИТЬ ТАБЛИЦУ MESSAGES В БД**

```sql
CREATE TABLE messages (
    id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(id),
    role VARCHAR(20),  -- 'user' или 'assistant'
    content TEXT,
    created_at TIMESTAMP DEFAULT NOW(),
    technique_used VARCHAR(50),
    emotion VARCHAR(50),
    metadata JSON
);

CREATE INDEX idx_messages_user_created ON messages(user_id, created_at);
```

**Сохранять каждое сообщение** в `state_manager.py:process_message()`:
```python
# После генерации ответа
await self.save_message(
    user_id=user_id,
    role="user",
    content=message,
    emotion=context.get("primary_emotion"),
    metadata={"distress_level": distress_level}
)

await self.save_message(
    user_id=user_id,
    role="assistant",
    content=response,
    technique_used=technique_name,
    metadata=result.metadata
)
```

### 4. **УЛУЧШИТЬ ROUTING В STATE GRAPH**

**Добавить логику прогрессии**:
```python
def _route_after_technique(self, state: Dict[str, Any]) -> str:
    """Route after technique execution based on dialogue progress."""
    user_state = state["user_state"]
    message_count = user_state.messages_count

    # После 3-5 сообщений active listening → предложить действия
    if message_count >= 5:
        # Проверить, достаточно ли информации
        themes = state.get("themes_discussed", [])
        if len(themes) >= 3:
            return "suggest_next_step"  # Новый узел

    # Продолжить слушание
    return "continue_listening"
```

### 5. **СОЗДАТЬ НОВУЮ ТЕХНИКУ: "TRANSITION TO ACTION"**

```python
class TransitionToAction(Technique):
    """Переход от слушания к действиям."""

    async def apply(self, user_message: str, context: Dict[str, Any]):
        # Суммировать услышанное
        # Предложить написать письмо или упражнение

        prompt = f"""Основываясь на диалоге, суммируйте ситуацию пользователя
        и предложите следующий шаг:
        - Написать письмо ребёнку
        - Попробовать упражнение по работе с эмоциями
        - Поставить цель

        История диалога:
        {format_history(context['message_history'])}
        """
```

### 6. **ДОБАВИТЬ МЕТРИКИ ПРОГРЕССА**

В таблице `users` добавить:
```sql
ALTER TABLE users ADD COLUMN dialogue_stage VARCHAR(50);
ALTER TABLE users ADD COLUMN letters_written INTEGER DEFAULT 0;
ALTER TABLE users ADD COLUMN exercises_completed INTEGER DEFAULT 0;
ALTER TABLE users ADD COLUMN last_topic VARCHAR(100);
```

---

## Приоритеты внедрения

### 🔥 **ВЫСОКИЙ ПРИОРИТЕТ** (сделать сейчас):
1. ✅ **Передача истории в LLM** - уже есть в памяти, нужно просто передать
2. ✅ **Улучшить system prompt** - добавить этапы диалога
3. ✅ **Добавить счётчик сообщений** - для определения этапа

### 🟡 **СРЕДНИЙ ПРИОРИТЕТ** (следующая итерация):
4. Создать таблицу `messages` и сохранять историю
5. Добавить технику "Transition to Action"
6. Улучшить routing с учётом прогресса

### 🟢 **НИЗКИЙ ПРИОРИТЕТ** (будущие улучшения):
7. Метрики прогресса пользователя
8. Персонализация промптов
9. A/B тестирование разных стилей

---

## Файлы для изменения

### Для улучшения стиля диалога:
1. `src/techniques/active_listening.py` - добавить историю в LLM
2. `src/orchestration/state_manager.py` - передать историю в context
3. `src/techniques/orchestrator.py` - добавить логику прогрессии

### Для сохранения истории:
1. `alembic/versions/XXX_add_messages_table.py` - новая миграция
2. `src/db/models.py` - добавить модель Message
3. `src/orchestration/state_manager.py` - методы save_message/load_history

---

## Пример улучшенного диалога

**До** (текущее):
```
User: Мне тяжело
Bot: Понимаю... Расскажите, что стало причиной?

User: 3 года не видел сына
Bot: Вижу, как тяжело... Что привело к этому?

User: Что мне делать
Bot: Понимаю... Какая ситуация заставила вас так чувствовать?
```

**После** (с историей и прогрессией):
```
User: Мне тяжело
Bot: Я слышу вашу боль. Расскажите, что происходит?

User: 3 года не видел сына
Bot: Три года без встреч с сыном — это невероятно тяжело.
     Что мешает вам видеться?

User: Что мне делать
Bot: Я понимаю вашу ситуацию: три года разлуки с сыном, сложные
     отношения с бывшей женой. Я вижу два пути поддержки:

     1. Написать письмо сыну — выразить свои чувства, любовь
     2. Упражнение по работе с гневом на бывшую

     Что вам ближе сейчас?
```
