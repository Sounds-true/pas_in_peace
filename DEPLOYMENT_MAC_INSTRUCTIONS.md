# 🚀 Инструкция по развертыванию на Mac (локально)

**Дата:** 2025-11-06
**Версия:** v1.0 (100% - все спринты готовы)

---

## 📋 ЧТО У ВАС ЕСТЬ:

- ✅ Старая папка с ботом (первая итерация)
- ✅ `.env` файл с токенами (`TELEGRAM_BOT_TOKEN`, `OPENAI_API_KEY`)
- ✅ Private репозиторий на GitHub: `Sounds-true/pas_in_peace`

---

## 🎯 ВАРИАНТ А: ОБНОВИТЬ СУЩЕСТВУЮЩУЮ ПАПКУ (РЕКОМЕНДУЮ) 🏆

**Когда использовать:** Если хотите сохранить `.env` и историю разработки

### Шаг 1: Сохранить .env

```bash
# Перейти в старую папку бота
cd ~/путь/к/старой/папке/pas_in_peace

# Скопировать .env в безопасное место
cp .env ~/.env_backup
echo "✅ .env сохранен в ~/.env_backup"
```

### Шаг 2: Обновить репозиторий

```bash
# Убедиться что на правильной ветке
git checkout main

# Получить последние изменения
git fetch origin main

# Сохранить локальные изменения (если есть)
git stash

# Обновить до последней версии
git pull origin main

# Вернуть локальные изменения (если были)
git stash pop

echo "✅ Репозиторий обновлен"
```

### Шаг 3: Восстановить .env

```bash
# Восстановить .env из backup
cp ~/.env_backup .env

# Проверить что токены на месте
grep "TELEGRAM_BOT_TOKEN" .env
grep "OPENAI_API_KEY" .env

echo "✅ .env восстановлен"
```

### Шаг 4: Установить зависимости

```bash
# Создать виртуальное окружение (если еще нет)
python3 -m venv venv

# Активировать
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

echo "✅ Зависимости установлены"
```

### Шаг 5: Проверить .env

```bash
# Посмотреть что в .env
cat .env

# Должны быть минимум:
# TELEGRAM_BOT_TOKEN=your_token_here
# OPENAI_API_KEY=sk-...

# Если чего-то не хватает, сравнить с примером:
cat .env.example
```

### Шаг 6: Запустить бота

```bash
# Убедиться что venv активирован
source venv/bin/activate

# Запустить бота
python src/bot.py

# Должны увидеть:
# [INFO] Bot started successfully
# [INFO] Database initialized
```

---

## 🆕 ВАРИАНТ Б: СОЗДАТЬ НОВУЮ ПАПКУ

**Когда использовать:** Если хотите чистую установку или старая папка сломана

### Шаг 1: Сохранить .env из старой папки

```bash
# Скопировать .env в домашнюю директорию
cp ~/путь/к/старой/папке/pas_in_peace/.env ~/.env_pas_backup
echo "✅ .env сохранен"
```

### Шаг 2: Клонировать репозиторий в новую папку

```bash
# Перейти в директорию для проектов (или создать)
cd ~/Projects  # или любая другая директория
mkdir -p ~/Projects  # создать если нет

# Клонировать репозиторий
git clone https://github.com/Sounds-true/pas_in_peace.git pas_in_peace_v1

# Перейти в новую папку
cd pas_in_peace_v1

echo "✅ Репозиторий склонирован"
```

### Шаг 3: Скопировать .env

```bash
# Скопировать сохраненный .env
cp ~/.env_pas_backup .env

# Проверить
cat .env | grep -E "TELEGRAM_BOT_TOKEN|OPENAI_API_KEY"

echo "✅ .env скопирован"
```

### Шаг 4: Установить зависимости

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать
source venv/bin/activate

# Обновить pip
pip install --upgrade pip

# Установить зависимости
pip install -r requirements.txt

echo "✅ Зависимости установлены"
```

### Шаг 5: Запустить бота

```bash
# Убедиться что venv активирован
source venv/bin/activate

# Запустить
python src/bot.py
```

---

## 🧪 ТЕСТИРОВАНИЕ БОТА

### Быстрая проверка (без запуска)

```bash
# Проверить что код компилируется
python -m py_compile src/bot.py
python -m py_compile src/orchestration/state_manager.py

echo "✅ Код компилируется без ошибок"
```

### Запустить unit тесты

```bash
# Активировать venv
source venv/bin/activate

# Запустить все тесты
pytest tests/ -v

# Или только важные:
pytest tests/test_safety_protocols.py -v
pytest tests/test_legal_tools.py -v

echo "✅ Тесты пройдены"
```

### Запустить scenario тесты

```bash
# Тесты для 7 эмоциональных состояний
pytest tests/scenarios/test_emotional_states.py -v

# Интеграционные тесты
pytest tests/integration/test_full_flow.py -v

echo "✅ Scenario тесты пройдены"
```

### Запустить red-team safety тесты

```bash
# Adversarial prompts (30+ тестов)
pytest tests/safety/test_red_team.py -v

echo "✅ Safety тесты пройдены"
```

### Запустить бота в тестовом режиме

```bash
# Запустить бота
python src/bot.py

# В другом терминале - отправить тестовое сообщение:
# Откройте Telegram → найдите вашего бота → напишите /start

# Проверить что бот отвечает
```

---

## 🐳 АЛЬТЕРНАТИВА: ЗАПУСК ЧЕРЕЗ DOCKER

Если хотите изолированное окружение:

```bash
# Скопировать .env в корень проекта
cp ~/.env_pas_backup .env

# Запустить через docker-compose
docker-compose up -d

# Проверить логи
docker-compose logs -f bot

# Остановить
docker-compose down
```

---

## 🔍 ПРОВЕРКА ЧТО ВСЕ РАБОТАЕТ

### 1. Проверить что бот запущен

```bash
# Должны видеть процесс
ps aux | grep "python src/bot.py"
```

### 2. Проверить логи

```bash
# Логи должны показывать:
# [INFO] Bot started successfully
# [INFO] Database connection established
# [INFO] StateManager initialized
# [INFO] Crisis detector ready
# [INFO] Legal tools loaded
```

### 3. Тестовый сценарий в Telegram

```
Вы → /start
Бот → Приветственное сообщение

Вы → Я очень злюсь на бывшего
Бот → Проявит эмпатию, предложит технику

Вы → Помоги написать сообщение
Бот → Предложит BIFF метод
```

---

## ⚠️ ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема: ModuleNotFoundError

```bash
# Решение: переустановить зависимости
pip install -r requirements.txt --force-reinstall
```

### Проблема: Database connection failed

```bash
# Проверить что PostgreSQL запущен (если используете)
# Или убедиться что бот работает без БД (hybrid mode)

# В .env добавить:
DATABASE_ENABLED=false
```

### Проблема: OpenAI API key invalid

```bash
# Проверить ключ
echo $OPENAI_API_KEY

# Или в .env:
cat .env | grep OPENAI_API_KEY

# Получить новый на: https://platform.openai.com/api-keys
```

### Проблема: Telegram bot не отвечает

```bash
# Проверить токен
cat .env | grep TELEGRAM_BOT_TOKEN

# Проверить что бот запущен
ps aux | grep bot.py

# Проверить логи на ошибки
```

---

## 📊 МЕТРИКИ ДЛЯ ПРОВЕРКИ

После запуска, убедитесь что:

- ✅ Бот отвечает на `/start`
- ✅ Crisis detection работает (тест: "не хочу жить")
- ✅ Legal tools доступны (тест: "помоги с дневником контактов")
- ✅ Therapeutic techniques работают (тест: "мне грустно")
- ✅ SupervisorAgent проверяет ответы
- ✅ Метрики собираются

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ ПОСЛЕ ЗАПУСКА

1. ✅ **Локально работает** → Переход к staging deployment
2. 📊 **Запустить load тесты** → `cd tests/load && locust -f locustfile.py`
3. 👨‍⚕️ **Clinical Advisory Board** → Показать бот терапевтам
4. 🎯 **Real bot testing** → Тестирование с реальными пользователями (ограниченная группа)
5. 🚀 **Production** → Полноценный запуск

---

## 📞 ПОМОЩЬ

**Если что-то не работает:**

1. Проверьте логи: `cat logs/bot.log` (если есть)
2. Запустите тесты: `pytest tests/ -v`
3. Проверьте .env: `cat .env`
4. Проверьте зависимости: `pip list | grep -E "langchain|openai|telegram"`

**Если нужна помощь с конкретной ошибкой:**
- Скопируйте полный traceback
- Проверьте requirements.txt
- Проверьте версию Python: `python --version` (нужна 3.10+)

---

**Готово! Бот должен работать локально.** 🎉

Следующий шаг: Staging deployment для real bot testing.
